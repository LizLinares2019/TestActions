name: docker-build-workflow

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-container:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps: 
      - uses: actions/checkout@v2
      - name: Build the Docker image        
        run: docker build . --file Dockerfile --tag my-app-by-docker:$(date +%s)
